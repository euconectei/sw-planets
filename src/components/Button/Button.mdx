---
name: Button
menu: Components
route: /components/button
---

import {Playground, Props} from 'docz';
import {BrowserRouter as Router, useHistory} from 'react-router-dom';
import Button from './Button';

# Button

Use Button styles for actions in forms, dialogs, and more

## Properties

<Props of={Button} />

## Basic Usage

<Playground>
  {() => {
    const onClick = () => false;
    return (
      <Router>
        <Button callback={() => onClick()} value="Next" />
      </Router>
    );
  }}
</Playground>
